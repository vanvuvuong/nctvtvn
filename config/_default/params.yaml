pwa:
  precache:
    images:
      - /images/logo.png
    pages:
      - /
metaRobots: "index, follow"
# Custom: docs left-nav always shows every part of the book, not just the active section
docs:
  nav:
    expand: false
    reduceFontSize: true
